//Header Guard
#pragma once

//Includes
#include <string>

#include <typedefs.hpp>

namespace OPTIONS
{
    //Debug Items
    const bool debugOptionsEncrypted = false;
    const string_t key = "";
    const string_t logPath = "bin/Log.txt";
}