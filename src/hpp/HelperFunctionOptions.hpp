//Header Guard
#pragma once

//Includes
#include <string>

namespace OPTIONS
{
    //Debug Items
    const bool debugOptionsEncrypted = false;
    const std::string key = "";
    const std::string logPath = "bin/Log.txt";
}